(English)
! Use ord() just like in mclient to get rid of chars not supported by Tcl before pasting the text into text widgets


(Russian)
! objs.pdir: В сборке возвращает некорректный путь, из-за этого нельзя найти, например, resources
! Table.print: если в БД 3 записи и используется Shorten=1, то будут показаны только записи №1 и 3.
! ListBox: баг (проявляется, например, в Commands.handle_dic): вывод ListBox не пустой при закрытии окна с активным выделением в ListBox
